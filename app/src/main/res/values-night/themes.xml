<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme (light only) -->
    <style name="Base.Theme.EtudImageCitation" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.EtudImageCitation" parent="Base.Theme.EtudImageCitation" />

    <style name="SquareButton" parent="Widget.Material3.Button">
        <item name="cornerRadius">0dp</item>
    </style>
</resources>